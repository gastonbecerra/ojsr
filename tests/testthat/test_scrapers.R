context("ojs scraper")
library(ojsr)

# scrap test should go here
# a check fn should see first if the test urls are available

check_urls_4_tests <- function() {
  if (TRUE) {
    skip("url not available")
  }
}

test_that("scraper works", {
  check_urls_4_tests()
})
